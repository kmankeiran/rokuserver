// this is our SQL connect information
// objects
const sqlCreds = {
    user: "root",
    password: "",
    host: "localhost",
    port: 3306,
    database: "roku_streaming"
}

module.exports = sqlCreds;